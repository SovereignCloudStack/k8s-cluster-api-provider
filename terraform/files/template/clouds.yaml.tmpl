---
clouds:
 ${cloud_provider}:
  auth:
   auth_url: ${clouds.auth.auth_url}
   username: ${clouds.auth.username}
   project_name: ${clouds.auth.project_name}
   user_domain_name: ${clouds.auth.user_domain_name}
   project_domain_name: ${clouds.auth.project_domain_name}
   password: ${secure.auth.password}
  region_name: ${clouds.region_name}
  interface: ${clouds.interface}
  identity_api_version: ${clouds.identity_api_version}
